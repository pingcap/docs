---
title: SIN
summary: 返回 `x` 的正弦值，其中 `x` 以弧度为单位。
---

# SIN

返回 `x` 的正弦值，其中 `x` 以弧度为单位。

## 语法 {#syntax}

```sql
SIN( <x> )
```

## 示例 {#examples}

```sql
SELECT SIN(90);

┌────────────────────┐
│       sin(90)      │
├────────────────────┤
│ 0.8939966636005579 │
└────────────────────┘
```