---
title: H3_TO_PARENT
summary: 返回包含分辨率为 `parent_res` 的 `h3` 的父索引。返回 0 表示发生了错误。
---

# H3_TO_PARENT

返回包含分辨率为 `parent_res` 的 `h3` 的父索引。返回 0 表示发生了错误。

## 语法 {#syntax}

```sql
H3_TO_PARENT(h3, parent_res)
```

## 示例 {#examples}

```sql
SELECT H3_TO_PARENT(635318325446452991, 12);

┌──────────────────────────────────────┐
│ h3_to_parent(635318325446452991, 12) │
├──────────────────────────────────────┤
│                   630814725819082751 │
└──────────────────────────────────────┘
```