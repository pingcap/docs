---
title: LOG2
summary: 返回 `x` 的以 2 为底的对数。如果 `x` 小于或等于 0.0E0，则该函数返回 NULL。
---

# LOG2

返回 `x` 的以 2 为底的对数。如果 `x` 小于或等于 0.0E0，则该函数返回 NULL。

## 语法 {#syntax}

```sql
LOG2( <x> )
```

## 示例 {#examples}

```sql
SELECT LOG2(65536);

┌─────────────┐
│ log2(65536) │
├─────────────┤
│          16 │
└─────────────┘
```