---
title: COT
summary: 返回 `x` 的余切值，其中 `x` 以弧度为单位。
---

# COT

返回 `x` 的余切值，其中 `x` 以弧度为单位。

## 语法 {#syntax}

```sql
COT( <x> )
```

## 示例 {#examples}

```sql
SELECT COT(12);

┌─────────────────────┐
│       cot(12)       │
├─────────────────────┤
│ -1.5726734063976895 │
└─────────────────────┘
```